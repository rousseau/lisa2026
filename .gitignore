# ── Données lourdes (jamais versionnées) ────────────────────────────────────
outputs/          # images, prédictions, checkpoints générés localement
nnUNet_preprocessed/
nnUNet_raw/
nnUNet_results/
external/         # MedSAM2, nnU-Net dependencies (managed separately via submodules)
*.nii
*.nii.gz
*.npy
*.npz
*.h5
*.hdf5
*.pt
*.pth
*.ckpt

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.so
.env
.venv
venv/

# ── Jupyter ──────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── LaTeX ────────────────────────────────────────────────────────────────────
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.pdf

# ── macOS ────────────────────────────────────────────────────────────────────
.DS_Store
._.DS_Store
doc/
# results/ est intentionnellement tracké (documentation scientifique légère)
# Seules les prédictions lourdes sont exclues :
results/**/*.csv

# Identifiants de synchronisation Jean Zay (jamais versionné)
.sync_env
